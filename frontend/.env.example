# URL base del backend de reportes desplegado en Railway.
# Si se deja vacío o se omite, la app funciona sin reportes PDF
# (el cálculo y la exportación a JSON siguen disponibles).
#
# Ejemplo:
# VITE_API_URL=https://signallab-backend.up.railway.app
VITE_API_URL=
